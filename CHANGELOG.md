# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的记录方式，版本号采用语义化版本格式。

## [0.6.0] - 2026-08-12

### 新增

- 新增紫薇桌宠、悬浮入口与手机比例界面。
- 新增玉简独立通讯录、主动传讯、联系人档案缓存及单条消息删除。
- 新增从现有状态栏导入玉简聊天记录的兼容入口；后续数据由脚本独立维护。
- 新增正文玉简通信解析：仅提取明确通过玉简、传讯玉符等媒介实际发送或收到的消息原文。
- 新增清空当前联系人的全部消息；不会影响其他联系人。
- 新增绝色榜、仙网风闻、仙网论坛和天机日报，以及各自的手动/自动推演设置。
- 新增主线注入独立开关，可分别控制玉简、风闻、论坛和日报是否影响后续正文。
- 新增仙网重 Roll 兼容开关及额外 API 调用提示。
- 新增删除多层聊天后的派生数据对账：自动清理失去来源的玉简、风闻、论坛和日报，并重新关联移动后的楼层编号；绝色榜不参与清理。
- 新增 MVU 世界时间、地点、人物关系、道侣亲密度、储物袋和地图的只读展示。

### 调整

- 玉简解析不受仙网重 Roll 开关控制；同一楼层出现不同 Swipe 时必定重新解析并替换该楼层旧结果。
- 仙网生成间隔按不同 AI 正文楼层计算，重 Roll 不累计层数；仅触发楼层的不同 Swipe 可按设置重新生成。
- 风闻、论坛和日报使用同一套仙网 API 配置；绝色榜与玉简分别使用独立配置。
- 正式导入文件与脚本显示名称统一为 `道渊小手机V0.6`。

### 修复

- 修复设置页“保存重 Roll 设置”按钮与上方选项贴合的问题。
- 修复人物“好感”、道侣“亲密”字段读取及离场联系人缓存展示。
- 修复手机模式下的布局、返回入口、滚动、长文本换行和暗色模型下拉显示。
- 修复手机软键盘弹出时前端按高度等比缩窄的问题；输入期间保持面板宽度、压缩可见高度并自动滚动聚焦字段，关闭键盘后恢复原位置与比例。视口尺寸变化不再重绘当前应用页面，避免输入框失焦并退回设置首页。
- 补充 Android Chrome 键盘识别：兼容布局视口与可视视口同时缩短的行为，不再仅依赖 iOS 的两者高度差，避免 API Key 聚焦后手机 UI 被等比缩窄。
- API 基础 URL 字段统一使用网址输入语义；Android Chrome 支持 Virtual Keyboard API 时，在用户点按后主动请求屏幕键盘，解决模拟器连接实体键盘时 URL 只显示输入法工具条的问题。
- 统一直连 API 的 Chat Completions 地址、模型列表地址和响应内容解析，兼容 `message.content` 字符串与内容数组；Responses API 暂不纳入直连兼容范围。
- 直连 API 改为按 Coding 地址自动适配 Anthropic Messages 与 OpenAI Responses 请求格式，用户无需选择协议；普通地址继续使用 Chat Completions。
- 兼容 OpenCode Zen 提供的完整 `/v1/chat/completions`、`/v1/responses` 和 `/v1/messages` 端点，避免重复拼接请求路径。
- 直连 API 在酒馆环境中优先通过酒馆后端 OpenAI 代理请求，绕过第三方服务的浏览器 CORS 限制；模型列表同步通过酒馆代理状态接口获取。
- 修正请求顺序：恢复浏览器直连优先，直连失败或 CORS 时才自动切换酒馆 OpenAI 代理；模型列表同样采用直连优先、代理兜底。
- 修复重 Roll 与楼层删除导致的自动内容重复、累积或来源楼层错位问题。
- 新增仙网自动生成倒计数，显示风闻、论坛和日报距离下一次自动生成还剩多少轮对话。
- 手动推演成功后，对应仙网模块的自动生成计数归零，重新按设置的楼层间隔计算。
- 将仙网倒计数改为操作栏下方的紧凑单行提示，避免占用大块内容区域。
- 修复新 AI 楼层已写入仙网计数、但已打开界面未立即刷新而仍显示旧倒计数的问题。

### 已知限制

- 正文玉简解析和自动仙网推演会产生额外 API 请求，具体费用取决于使用者配置的服务。
- 公开发布前仍需完成正式 JSON 在目标 SillyTavern 版本中的完整桌面/手机回归测试。
- 紫薇桌宠为本项目通过 AI 图像工具原创生成的项目资产；绝色榜牌匾仍需在公开分发前替换为原创或具有明确授权的素材。
- 使用建议：优先使用桌面端；手机端仅完成有限兼容验证，不保证所有浏览器、模拟器和软键盘环境稳定。
