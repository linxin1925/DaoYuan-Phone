# 长期路线

## V1.5 Vue/UI 组件化重构

状态：作为 V0.7 的并行开发线规划，不影响 V0.7 稳定版本。

V0.7 继续采用当前稳定实现并独立维护。V1.5 从 V0.7 正式源码建立独立分支，使用 Vue 和 UI skill 重构 iframe 内 UI，并增加手机/桌面平板双布局。

V1.5 的详细计划见 [实施计划](docs/v1.5/实施计划.md)，进度记录见 [实施进度](docs/v1.5/实施进度.md)。迁移采用分阶段方式，不一次性重写全部界面。

启动和推进 V1.5 时，重点检查以下条件：

- 手机应用、设置页面和弹窗数量明显增加。
- 页面状态、草稿状态及跨页面联动难以继续由手工 DOM 更新维护。
- 数据刷新频繁引发整页重绘、焦点丢失、滚动位置丢失等问题。
- `renderUi.ts` 的修改冲突和回归风险持续升高。
- 需要建立可复用组件、统一表单校验或更细粒度的响应式更新。

V1.5 遵循以下边界：

1. 保持酒馆助手正式 JSON 的导入方式与运行入口不变。
2. 保持 Tavern Helper、MVU、世界书和聊天变量协议不变。
3. 按应用逐个迁移，不一次性重写全部界面。
4. 优先迁移状态复杂且仍在频繁扩展的页面。
5. 每个阶段都重新验证桌面、iPhone、Android、重 Roll、聊天切换及输入焦点。
6. Vue 运行时必须内联进 V1.5 测试 JSON，不增加玩家侧安装步骤。
7. V1.5 未完成验收前，不覆盖 V0.7 正式 JSON，也不要求 V0.7 合并 Vue 代码。
