# 道渊功能前端使用说明

## 一、功能概览

本工具以悬浮玉简运行，包含传讯、绝色榜、仙网风闻、论坛、日报、储物袋和地图。各页从桌面进入，底部导航可快速返回常用功能。

使用建议：优先在桌面端使用。手机端只进行了有限兼容验证，部分浏览器、模拟器或软键盘环境可能出现输入法、视口缩放或布局问题。

## 二、数据来源

### 2.1 世界状态

时间、地点、人物关系和储物袋来自当前楼层的 MVU `stat_data`。前端只读展示，不修改人物、物品或世界变量。

### 2.2 玉简传讯

联系人来自当前 `stat_data.人物`、`stat_data.道侣` 及本工具保存的已认识人物档案，立绘库只负责匹配头像，不会把陌生角色自动加入通讯录。生成回复时会读取该角色的世界书资料、当前人物档案、近期主线正文和双方传讯历史，使回复符合当前剧情与人设。联系人档案和消息保存在浏览器本地，并按 chat 隔离；不会写入或覆盖状态栏玉简、人物、道侣等 MVU 数据。

玉简角色可以了解近期正文。若用户在“设置 → 主线注入”开启“玉简传讯”，近期私下通信也会提供给后续主线 AI，使人物知情与剧情反应保持连贯；关闭时仍保持单向读取。

在“设置 → 玉简传讯 API”中可开启“解析正文中的玉简通信”。开启后，每个 AI 正文层完成时会使用玉简独立 API，但只接受正文中明确出现玉简、传讯玉符等远程媒介，且已经实际发送或收到的消息原文。解析器不得把动作、意图、结果、摘要、当面对话、传音、心理活动或准备发送的内容改写成聊天气泡；无法定位消息原文时必须忽略。系统按正文消息 ID 去重，同一层不会重复入账。状态栏玉简仅供手动导入旧档，后续独立解析不依赖状态栏。

### 2.3 绝色榜

绝色榜依据当前界域和角色世界书，从有资料的候选人中随机抽取五人，调用独立 API 推演新榜。旧榜整体替换，不持续累加。榜单生成和群芳谱回帖常驻使用破甲提示词，但仍服从既定格式、世界设定和安全边界。

### 2.4 仙网内容

风闻、论坛和日报共用仙网内容 API。推演时读取近期正文、时间地点、世界事实和世界书。风闻可能失真，论坛代表道友观点，日报属于世界内媒体叙事，均不等同于 MVU 事实。

## 三、数据隔离与不共享范围

绝色榜、仙网内容和玉简传讯的 API 配置彼此独立，不共享密钥、模型、提示词或返回内容。仙网三项内容分别保存到当前 chat 变量，不写入 `stat_data`。联系人和传讯历史保存在浏览器本地，不写回状态栏。API 密钥只存于本地设置，不进入聊天变量、世界书或提示词。

切换聊天后只读取对应 chat 数据，不跨聊天共享。删除风闻、帖子或报纸不会影响正文、MVU、绝色榜和其他板块。

### 3.1 正文读取与注入关系

“设置 → 主线注入”提供玉简传讯、仙网风闻、仙网论坛、天机日报四个独立开关，默认全部关闭。开启的模块会把当前聊天内的近期内容加入后续正文 AI 上下文；关闭的模块完全不参与。系统自动限制条目和文本长度，并明确标注信息性质：玉简是通信记录，风闻未经证实，论坛是个人言论，日报是媒体叙事。注入只影响模型理解，不会直接写改 `stat_data`，也不会把传闻提升为世界事实。

## 四、自动推演与内容清理

自动推演只在 AI 正文完成、MVU 开始解析该 AI 正文层时触发；玩家发送消息时不会计数或请求，避免一轮对话重复生成两次。

“设置 → 仙网重 Roll 兼容”默认关闭。开启后，若同一 AI 楼层重 Roll 出不同正文，只有原本已达到生成间隔的仙网模块才会再次请求 API，并用新 Swipe 的结果替换该楼层旧结果；重 Roll 本身不增加楼层计数，因此间隔设为 3 时，同一楼层连续重 Roll 三次不会被当作三层。每次需要重新生成的重 Roll 都会产生额外 API 调用与可能的费用。玉简正文解析不受此开关控制：每个不同 Swipe 都必定重新调用玉简解析 API，并替换该楼层上一 Swipe 自动提取的消息。

### 4.1 四个独立自动开关

绝色榜、仙网风闻、仙网论坛和天机日报分别设有“启用自动推演”开关。关闭任一开关，只停止该模块随正文自动发起的 API 请求，不影响另外三个模块，也不会删除已有内容或清空原有间隔设置。关闭自动推演后，用户仍可进入对应页面手动点击推演；重新开启后从后续 AI 正文层继续计数。玉简的“解析正文中的玉简通信”是另一项独立开关，仅控制正文通信提取，不影响面板内主动传讯。

### 4.2 间隔、生成量与保留上限

间隔 `1` 表示每个 AI 正文层触发一次，`2` 表示每两个 AI 正文层触发一次；`0` 仍作为兼容性的关闭方式，但建议直接使用独立开关。仙网风闻每次在“最少生成条数”与“最多生成条数”之间随机取值；论坛按“每次生成帖子数”生成；日报按“每次生成期数”生成，期号由本地系统连续编排，不采用 AI 返回的重复期号。三项仙网功能分别计算间隔、生成量和保留上限。

内容超过保留上限后只清除最旧的对应内容，新内容始终置顶。进入板块即视为已读，之后桌面角标只显示新产生且尚未阅读的数量。删除风闻、帖子或日报只影响所选条目，不会改变正文、MVU 状态或其他模块的数据。

### 4.3 仙网互动与生成

设置页另有“紫薇桌宠大小”，提供大、中、小三档；大档为默认尺寸，修改只影响悬浮桌宠，不影响玉简窗口。

仙网设置提供去中心化模式、自动 AI 回复、点赞/热度显示、列表评论预览和破甲提示词开关。基础模式默认不选择主角相关事件，只有已经形成跨地域公共影响时才允许最多出现一条；去中心化模式则严格禁止本批出现主角相关内容。生成会读取当前角色绑定世界书及附加世界书中所有规则/背景类型（`constant`）条目，无论条目当前是否禁用，并完整发送条目正文；普通人物关键词条目不会整批输入。仙网生成读取 MVU 世界事实时会排除状态栏 `世界.动向`、`世界动向` 和 `动态` 字段，后续取消状态栏动向不会被仙网继续继承。评论数量设为 `0` 时，风闻和论坛不再生成附带评论。论坛详情可由用户发表评论；开启自动 AI 回复后，仙网模型会生成道友跟帖。风闻、论坛和日报的点赞可手动切换并保存到当前 chat。破甲提示词不会取消 JSON 格式、世界设定和安全边界。

## 五、使用建议

首次使用请在设置中填写 API 地址、密钥和模型，并手动推演一次。额度有限时可提高触发间隔；世界书有效候选不足五人时，绝色榜会停止并提示，不会补造人物。
